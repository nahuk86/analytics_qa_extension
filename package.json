{
  "name": "analytics-qa-extension",
  "version": "1.0.0",
  "description": "Adobe Analytics QA Chrome Extension – automated hit validation",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/.bin/jest --testPathPattern=tests/"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
